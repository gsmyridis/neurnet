# `neurnet`

Experiments with different neural network architectures.
Currently, it covers:

- Convolutional Neural Networks (CNN)
  - LeNet: training, evaluation and visualisation of feature maps.
  - AlexNet: fine tuning and evaluation.
